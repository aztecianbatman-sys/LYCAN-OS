The release installer is produced by frontend/electron-builder.

Windows x64 is the supported release architecture for LYCAN 1.0.

The installer installs a normal Windows application. It does not replace Windows or modify boot partitions.
