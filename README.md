# LYCAN OS

Native Windows-hosted virtual operating system project. LYCAN runs as a normal Windows application and does not replace the host Windows installation.

This repository contains the LYCAN OS 0.4.0 source snapshot and its Windows CI/build pipeline.
