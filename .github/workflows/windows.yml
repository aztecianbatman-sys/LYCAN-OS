name: Windows build
on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S . -B build -A x64
      - name: Build
        run: cmake --build build --config Release --parallel
      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure
      - name: Install Inno Setup
        shell: powershell
        run: choco install innosetup -y --no-progress
      - name: Package installer
        shell: cmd
        run: iscc installer\LycanOS.iss
      - name: Verify installer
        shell: powershell
        run: if (!(Test-Path installer\artifacts\LycanOS-Setup-1.0.0.exe)) { throw 'Installer missing' }
      - uses: actions/upload-artifact@v4
        with:
          name: LycanOS-1.0.0-Windows
          path: installer/artifacts/LycanOS-Setup-1.0.0.exe
